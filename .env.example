# Provisioned by `CONVEX_AGENT_MODE=anonymous npx convex dev --once` locally.
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=

# Clerk is required only for staff routes. Public customer/provider links remain account-free.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
CLERK_JWT_ISSUER_DOMAIN=

# No real provider exists in this MVP. Production mode fails closed.
ONE_NUMBER_COMMUNICATIONS_ADAPTER=mock
ONE_NUMBER_DEMO_MODE=false
